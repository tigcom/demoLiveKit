# Frontend Environment Variables
# These are loaded by Vite during build and runtime

# LiveKit Server Configuration
# VITE_LIVEKIT_URL: WebSocket URL to your LiveKit server
# Note: Must be prefixed with VITE_ for Vite to expose it to browser
VITE_LIVEKIT_URL=wss://your-livekit-server.livekit.cloud

# Token Server Configuration (backend that issues LiveKit tokens)
VITE_TOKEN_SERVER_URL=http://localhost:8000
